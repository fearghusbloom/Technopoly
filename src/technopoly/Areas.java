package technopoly;

/**
 * 
 * @author fearg
 *
 * All the valid values for board areas are specified here. Each of these areas belongs to a field.
 * To expand the board, simply add the new areas to the enum specification below.
 */
public enum Areas {
	HTML, CSS, GAMELOGIC, GRAPHICS, THREEDDESIGN, MOBILITY, SENSORS, MANIPULATION, RISKANALYTICS, BACKUPDATAANDRECOVERY
}
